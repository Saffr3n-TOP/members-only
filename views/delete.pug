extends layout

block content
  h1= title
  p(style='color: crimson; font-weight: bold; text-align: center;') Are you sure you want to delete this message? This action is irreversible!

  form(method='POST')
    button(type="submit") Confirm
